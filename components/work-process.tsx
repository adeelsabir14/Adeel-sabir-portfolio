"use client"

import { motion } from "framer-motion"
import { Reveal, Stagger, StaggerItem } from "@/components/motion-layer"

const steps = [{ number: "01", title: "Find the signal", text: "We get clear on the user, the business goal, and what success needs to look like." }, { number: "02", title: "Make the shape", text: "I turn the idea into a focused interface and technical plan before expensive decisions are made." }, { number: "03", title: "Ship with intent", text: "The product is built in visible increments, with quality and communication baked into the work." }, { number: "04", title: "Keep it moving", text: "After launch, we measure what matters and improve the pieces that create real momentum." }]

export default function WorkProcess() { return <section id="process" className="border-b border-border px-5 py-24 sm:px-8 lg:px-12"><div className="mx-auto max-w-[1400px]"><Reveal><div className="mb-14 flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="mb-5 font-mono text-xs uppercase tracking-[0.24em] text-primary">How I work</p><h2 className="display max-w-2xl text-5xl font-medium sm:text-7xl">Calm process. <span className="text-muted-foreground">Sharp output.</span></h2></div><p className="max-w-xs text-sm leading-6 text-muted-foreground">No mystery handoffs. No unnecessary ceremony. Just a clear path from problem to product.</p></div></Reveal><Stagger className="grid border-y border-border md:grid-cols-4">{steps.map((step) => <StaggerItem key={step.number}><motion.article whileHover={{ backgroundColor: "rgba(227,59,50,.05)" }} className="border-b border-border py-7 last:border-b-0 md:border-b-0 md:border-r md:px-7 md:first:pl-0 md:last:border-r-0 md:last:pr-0"><motion.p whileHover={{ x: 8 }} className="font-mono text-xs text-primary">{step.number}</motion.p><h3 className="mt-12 text-2xl font-medium tracking-tight">{step.title}</h3><p className="mt-4 text-sm leading-6 text-muted-foreground">{step.text}</p></motion.article></StaggerItem>)}</Stagger></div></section> }
